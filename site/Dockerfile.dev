FROM node:22-alpine
WORKDIR /app
COPY package*.json ./
RUN npm install
COPY . .
EXPOSE 5173
# --host 0.0.0.0 lets Docker forward the port to the host machine
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
